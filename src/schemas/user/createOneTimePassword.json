{
  "title": "User Model - Create One-Time Password",
  "description": "Schema definition for User Model - createOneTimePassword method",
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "walletId": {
      "description": "Wallet ID",
      "type": "string"
    },
    "smsNotification": {
      "description": "Indicates if corresponds sms notification (true) or email notification (false)",
      "type": "boolean"
    },
    "digits": {
      "description": "Number of digits",
      "type": "integer",
      "minimum": 1
    }
  },
  "required": [ "walletId" ]
}
