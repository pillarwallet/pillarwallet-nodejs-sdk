{
  "title": "User Model - update",
  "description": "Schema definition for User Model - Update method",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "walletId": {
      "description": "The wallet ID",
      "type": "string"
    },
    "username": {
      "description": "The username",
      "type": "string"
    },
    "firstName": {
      "description": "The first name of one user",
      "type": "string"
    },
    "lastName": {
      "description": "The last name of one user",
      "type": "string"
    },
    "email": {
      "description": "The user's email",
      "type": "string",
      "format": "email"
    },
    "phone": {
      "description": "The user's phone",
      "type": "string"
    },
    "country": {
      "description": "The user's country",
      "type": "string"
    },
    "state": {
      "description": "The user's state",
      "type": "string"
    },
    "city": {
      "description": "The user's city",
      "type": "string"
    },
    "tagline": {
      "description": "The user's tagline",
      "type": "string"
    },
    "taglineStatus": {
      "description": "Conditional for tagline",
      "type": "boolean"
    },
    "userSearchable": {
      "description": "Conditional for this user be searchable by others",
      "type": "boolean"
    },
    "profileImage": {
      "description": "The user's profileImage",
      "type": "string",
      "format": "uri"
    },
    "status": {
      "description": "The user's status",
      "type": "string"
    },
    "verificationService": {
      "description": "The verification Service",
      "type": "string"
    },
    "verificationStatus": {
      "description": "The verification status",
      "type": "string"
    },
    "verificationReference": {
      "description": "The verification reference",
      "type": "string"
    },
    "investorClassification": {
      "description": "The user's classification as investor",
      "type": "string"
    }
  },
  "required": ["walletId"]
}


