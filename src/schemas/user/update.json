{
  "title": "User Model - Update",
  "description": "Schema definition for User Model - update method",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "walletId": {
      "description": "The wallet ID",
      "type": "string"
    },
    "firstName": {
      "description": "The first name of one user",
      "type": "string"
    },
    "lastName": {
      "description": "The last name of one user",
      "type": "string"
    },
    "email": {
      "description": "The user's email",
      "type": "string"
    },
    "phone": {
      "description": "The user's phone",
      "type": "string"
    },
    "country": {
      "description": "The user's country",
      "type": "string"
    },
    "state": {
      "description": "The user's state",
      "type": "string"
    },
    "city": {
      "description": "The user's city",
      "type": "string"
    },
    "tagline": {
      "description": "The user's tagline",
      "type": "string"
    },
    "taglineStatus": {
      "description": "Conditional for tagline",
      "type": "boolean"
    },
    "userSearchable": {
      "description": "Conditional for this user be searchable by others",
      "type": "boolean"
    },
    "betaProgramParticipant": {
      "description": "Flag to check if user is beta program participant",
      "type": "boolean"
    },
    "profileImage": {
      "description": "The user's profile picture",
      "type": [ "string", "null" ]
    }
  },
  "required": ["walletId"]
}
