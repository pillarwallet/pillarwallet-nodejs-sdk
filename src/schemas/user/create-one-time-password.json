{
  "title": "User Model - Create OTP",
  "description": "Schema definition for User Model - Create OTP method",
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "phone": {
      "description": "The phone associated of one user",
      "type": "string",
      "pattern":"^\\+(?:[0-9] ?){6,14}[0-9]$"
    },
    "function": {
      "description": "The username",
      "type": "string",
      "enum": ["new", "import", "restore"]
    },
    "isVerifiedUser": {
      "description": "Conditional for verified user",
      "type": "boolean"
    }
  },
  "required": ["phone", "function", "isVerifiedUser"]
}
