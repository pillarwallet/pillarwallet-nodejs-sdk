{
  "title": "Assets Model - Defaults",
  "description": "Schema definition for Asset Model - Defaults method",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "walletId": {
      "description": "The wallet ID",
      "type": "string"
    }
  },
  "required": [ "walletId" ]
}
