{
    "title": "Connection Model - Disconnect",
    "description": "Schema definition for Connection Model - disconnect method",
    "additionalProperties": false,
    "type": "object",
    "properties": {
      "targetUserId": {
        "description": "The target user ID",
        "type": "string"
      },
      "sourceUserAccessKey": {
        "description": "The shared access key",
        "type": "string"
      },
      "targetUserAccessKey": {
        "description": "The shared access key",
        "type": "string"
      },
      "walletId": {
        "description": "The wallet ID",
        "type": "string"
      }
    },
    "required": [ "targetUserId", "sourceUserAccessKey", "targetUserAccessKey", "walletId" ]
  }
