{
  "title": "Connection Model - Accept",
  "description": "Schema definition for Notification Model - Accept method",
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "targetUserId": {
      "description": "The target user ID",
      "type": "string"
    },
    "accessKey": {
      "description": "The shared access key",
      "type": "string"
    },
    "walletId": {
      "description": "The wallet ID",
      "type": "string"
    }
  },
  "required": [ "targetUserId", "accessKey", "walletId" ]
}
